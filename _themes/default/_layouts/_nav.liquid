<nav class="navbar navbar-expand-lg navbar-light border-bottom">

    <div class="container">

        <a class="navbar-brand" href="{{ root }}/">
            <img src="{{ root }}/images/gaius-logo.png" style="height: 50px;" />
        </a>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto mr-3">

                <li class="nav-item {% if pageId == 'index' %} active {% endif %}">
                    <a class="nav-link" href="{{ root }}/">Home 
                        {% if pageId == 'index' %}
                            <span class="sr-only">(current)</span>
                        {% endif %}
                    </a>
                </li>

                <li class="nav-item {% if pageId == 'guides.getting-started' %} active {% endif %}">
                    <a class="nav-link" href="{{ root }}/guides/getting-started.html">Getting Started
                        {% if pageId == 'guides.getting-started' %}
                            <span class="sr-only">(current)</span>
                        {% endif %}
                    </a>
                </li>

                <li class="nav-item {% if pageId == 'guides.authoring-content' %} active {% endif %}">
                    <a class="nav-link" href="{{ root }}/guides/authoring-content.html">Authoring Content
                        {% if pageId == 'guides.authoring-content' %}
                            <span class="sr-only">(current)</span>
                        {% endif %}
                    </a>
                </li>

            </ul>
        </div>

    </div>

</nav>